{
  "name": "encore_playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "createdata":"npx playwright test --project=create_data",
    "clean:allure-results": "rimraf allure-results", 
    "ios": "npx playwright test --project=Mobile_Safari",
    "mobile_chrome": "npx playwright test --project=Mobile_Chrome",
    "ui": "npx playwright test --project=chromium",
    "clean-results":"npm run clean:allure-results",
    "allure:generate": "allure generate allure-results --clean",
    "allure:open": "allure open",
    "all":"npm run clean:allure-results & npm run ui & npm run mobile_chrome & npm run ios"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.47.1",
    "@types/node": "^22.5.5",
    "allure-commandline": "^2.30.0",
    "allure-playwright": "^2.15.1",
    "axios": "^1.7.7",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "atob": "^2.1.2",
    "dotenv": "^16.4.5"
  }
}
